global:
  resolve_timeout: 1m

route:
  group_by: ['alertname']
  receiver: 'tele-webhook'
  group_wait: 30s
  group_interval: 30s
  repeat_interval: 1m

receivers:
  - name: 'tele-webhook'
    webhook_configs:
      - url: 'http://tele-webhook:3005/alert'
        send_resolved: true